import SinglePathIcon, { SinglePathIconProps } from './SinglePathIcon';

export default function ChevronDownIcon(props: SinglePathIconProps) {
  return <SinglePathIcon d="M6 9L12 15L18 9" strokeWidth={3} {...props} />;
}
